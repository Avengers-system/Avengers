<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
 
<mapper namespace="professor">
	<select id="getProfessor" parameterType="java.lang.String" resultType="com.avengers.db.dto.PrfsVO">
		SELECT *
		FROM PROFESSOR
		WHERE PRFS_NUM= #{prfs_num}
	</select>

	<update id="updateProfessor" parameterType="com.avengers.db.dto.PrfsVO">
		UPATE PROFESSOR
		SET 
			
		WHERE PRFS_NUM = #{prfs_num}
	</update>
</mapper> 