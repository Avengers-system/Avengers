<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
	 PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	 "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="lct">
	
	<select id="getLct" parameterType="String" resultType="com.avengers.db.dto.LctVO">
		select * from lecture where lct_num =#{lct_num}
	</select>
	
</mapper>