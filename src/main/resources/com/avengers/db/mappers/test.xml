<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
	 PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	 "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.system.avengers.TestVO">

	<!-- id의 경우에는 Mapper.java에 동일하게 사용해야된다. -->
	<select id="getTestList" resultType="com.system.avengers.TestVO">
		SELECT * FROM SEqurity
	</select>
	
	<select id="getTest" parameterType="com.system.avengers.TestVO" resultType="com.system.avengers.TestVO">
		select * from SEqurity where id=#{id} and pwd= #{pwd}
	</select>
	
</mapper>